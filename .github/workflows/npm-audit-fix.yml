name: Monthly npm audit fix

on:
  schedule:
    # 03:00 UTC on the 1st of each month
    - cron: "0 3 1 * *"
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  audit-fix:
    uses: Plasius-LTD/plasius-ltd-site/.github/workflows/package-npm-audit-fix.yml@main
